[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
psycopg2-binary = "*"
bcrypt = "==4.0.1"
pyjwt = "*"
passlib = {extras = ["bcrypt"], version = "*"}
httpx = "*"
fastapi = "*"
uvicorn = {extras = ["standard"], version = "*"}
sqlalchemy = ">=2"
pydantic = ">=2"
jinja2 = "*"
python-dotenv = "*"
email-validator = "*"
psycopg = {extras = ["binary"], version = "*"}

[dev-packages]

[requires]
python_version = "3.13"
python_full_version = "3.13.5"

[scripts]
start = "python -X utf8 -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload"
